# Port the server listens on (Render sets this automatically).
PORT=4000

# Allowed client origin(s) for CORS. Comma-separated, or "*" for any (dev only).
# Example for production: CLIENT_ORIGIN=https://your-app.vercel.app
CLIENT_ORIGIN=*
